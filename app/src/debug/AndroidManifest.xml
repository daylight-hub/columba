<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="network.reticulum.android" />

    <application>
        <!-- Test host for Compose instrumented tests (no launcher icon) -->
        <activity
            android:name="network.libertychat.app.test.TestHostActivity"
            android:exported="true" />

        <!-- Disable LeakCanary's "Leaks" launcher icon -->
        <activity
            android:name="leakcanary.internal.activity.LeakLauncherActivity"
            android:enabled="false"
            tools:replace="android:enabled" />

        <!-- Debug-only: exposes a BroadcastReceiver that the columba phone
             harness drives via `adb shell am broadcast`. See TestReceiver.kt
             for the full action contract. Compiled out of release builds. -->
        <receiver
            android:name="network.libertychat.app.test.TestReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="network.libertychat.test.GET_DEST" />
                <action android:name="network.libertychat.test.HAS_PATH" />
                <action android:name="network.libertychat.test.SEND_DIRECT" />
                <action android:name="network.libertychat.test.SEND_OPP" />
                <action android:name="network.libertychat.test.SEND_PROP" />
                <action android:name="network.libertychat.test.GET_MSG_STATE" />
                <action android:name="network.libertychat.test.GET_RX" />
                <action android:name="network.libertychat.test.RX_CLEAR" />
                <action android:name="network.libertychat.test.ANNOUNCE" />
                <action android:name="network.libertychat.test.LIST_INTERFACES" />
                <action android:name="network.libertychat.test.DISABLE_ALL_INTERFACES" />
                <action android:name="network.libertychat.test.DISABLE_INTERFACE" />
                <action android:name="network.libertychat.test.ENABLE_INTERFACE" />
                <action android:name="network.libertychat.test.ADD_TCP_CLIENT" />
                <action android:name="network.libertychat.test.REMOVE_INTERFACE" />
                <action android:name="network.libertychat.test.SET_PROP_NODE" />
                <action android:name="network.libertychat.test.SYNC_PROP" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
